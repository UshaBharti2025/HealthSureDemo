/**
 * The `Gender` enum defines the gender options for individuals (e.g., doctors, providers) in the system.
 * 
 * It provides two values:
 * 
 * - `MALE`: Represents a male individual.
 * - `FEMALE`: Represents a female individual.
 * 
 * This enum is used to categorize and store the gender of a doctor or a provider within the healthsure system.
 */
package com.infinite.jsf.provider.model;

public enum Gender {
	MALE,FEMALE
}
