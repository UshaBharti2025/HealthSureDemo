/**
 * The `DoctorStatus` enum represents the possible statuses of a doctor within the system.
 * 
 * It provides two distinct states:
 * 
 * - `ACTIVE`: The doctor is currently active and available for practice.
 * - `INACTIVE`: The doctor is not currently practicing or is inactive in the system.
 * 
 * This enum is typically used to track and control the availability of doctors within a healthcare provider system.
 */
package com.infinite.jsf.provider.model;

public enum DoctorStatus {
	ACTIVE,INACTIVE

}
